#include <stdio.h>
#include <stdint.h>

int main (void) {
	int age;
	printf("Enter your age: ");
	scanf("%i",age);
	if (age < 18) printf("\nYou can't vote\n");
	else printf("\nYou can vote\n";)
}

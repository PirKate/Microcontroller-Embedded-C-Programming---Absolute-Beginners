#include <stdio.h>
#include <stdint.h>

int main (void) {
	int64_t age;

	printf("Enter your age: ");

	fflush(stdout);
	scanf("%I64u",&age);

	if (age < 18) printf("\nYou can't vote\n");
	else printf("\nYou can vote\n");

	while(getchar() != '\n') {

	}
	getchar();
}
